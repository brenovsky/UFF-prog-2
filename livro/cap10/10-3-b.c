#include <stdio.h>

int main()
{
    int voo[37], disponivel[37];
    int id, n; //n -> nº do voo escolhido
    int i;
    int resposta[10000];

    for (i = 0; i < 37; i++) {
        scanf("%d%d", &voo[i], &disponivel[i]);
    }

    i = 0;

    while (1) {// lendo documento e voo
        scanf("%d%d", &id, &n);

        if (id == 9999) {
            break;
        }

        if()
    }

    for


}